- [P0] maxbridge/config.py:67 (and :81) — non-dict-but-valid JSON in config.json → uncaught AttributeError at dock construction on Max's main thread — TRIGGER: `%LOCALAPPDATA%\MaxGaffer\config.json` contains `null`, `[]`, `"str"` or `42` (hand-edit, crashed sync tool, downgraded version writing a scalar) → next launch calls `cfgmod.load()` from `MaxGafferDock.__init__` (ui/dock.py:107, no guard) → `json.load` SUCCEEDS (no ValueError) → `d.items()` raises AttributeError, which is not in `except (OSError, ValueError)`; same defect in `_borrow_maxdirector_key` (`json.load(f).get("api_key")` on a non-dict MaxDirector config when api_key is empty) — EVIDENCE: lines 64–71 catch only OSError/ValueError; AttributeError propagates out of the dock constructor, killing the plugin load on the main thread — FIX: `if not isinstance(d, dict): d = {}` before the loop (and `isinstance(..., dict)` guard on the borrowed config), or widen the except to `Exception`.

- [P1] maxbridge/scene.py:306-327 — `set_dome_texture` mutates the scene, THEN can report "failed" — TRIGGER: dome has no texmap and none of the `DOME_TEX_FILE` candidates (`HDRIMapName` etc. — README checklist #16 is still unverified on-box) exists on the created VRayBitmap → function creates the texmap, assigns `dome.texmap = tex` (line 321), then `set_prop(tex, DOME_TEX_FILE, …)` returns None → returns "failed" with an EMPTY texmap now bound to the dome (dome renders black / prior implicit env lost) while callers (controller.py:831) treat "failed" as no-change — EVIDENCE: assignment at line 321 is not rolled back when line 324–326 fails; no try/finally or revert — FIX: capture the previous texmap first; on any failure after assignment, restore it (`dome.texmap = prev` best-effort) before returning "failed".

- [P2] maxbridge/digest.py:95-112 — one hostile light aborts enumeration of ALL remaining lights — TRIGGER: a scene with a light node whose `rt.classOf(lt)` (line 97) or `str(lt.name)` (line 101) raises (stale handle in a corrupted scene, exotic plugin light) → the single outer try swallows it and the loop ends; digest silently reports a partial/empty lights list → LLM change-plan is ungrounded for the missing lights — EVIDENCE: only `layer` (103) and `pos` (107) are per-light guarded; classOf/name are not — FIX: wrap the per-light body in its own try/except and continue.

- [P2] maxbridge/scene.py:68-77 — `camera_yaw_deg` swallows every exception and returns 0.0 — TRIGGER: any transform read hiccup (stale node handle, corrupted TM) → yaw silently becomes 0°, which feeds `rules.initial_state` sun azimuth (controller.py:448) and `camera_basis` look point for LLM light placement (digest.py:143) → wrong azimuth with no log trace — EVIDENCE: bare `except Exception: return 0.0` with no note — FIX: return `Optional[float]` (None) or log a warning so callers can skip yaw-derived guesses.

- [P2] maxbridge/scene.py:211-234 — fabricated sun geometry on read failure instead of an error — TRIGGER: sun handle goes stale mid-session (user deletes sun; controller caches `self._rig`) → `_sun_pivot` falls back to world origin (0,0,0) and `read_sun_angles` returns the made-up triple `(0.0, 45.0, 10000.0)` (line 229) → `write_sun_angles` then positions the sun on a 10000-unit orbit around the WORLD ORIGIN rather than its real target — silent wrong scene mutation; only if the write itself throws does a warning appear — FIX: make the fallback `None` and have callers emit a "sun unreadable" rig note instead of fabricating angles.

- [P2] maxbridge/scene.py:98-102 — duplicate node names silently target the wrong camera — TRIGGER: two cameras named "Cam01" (Max permits this; merged scenes often produce it) → `list_cameras` shows both, but `get_camera` → `getNodeByName(name, exact=True)` always returns the FIRST; state apply, exposure-host writes and digest props all land on the wrong camera with no warning — FIX: detect duplicates in `list_cameras` (name-count) and surface a note; prefer identity-by-handle over name lookup where possible.

- [P2] maxbridge/config.py:67-69 — loaded values are never type-checked — TRIGGER: `"loop_width": "wide"` or `"critic_weights": "x"` in config.json → `setattr` stores the wrong type; the TypeError surfaces later in an unrelated module (render sizing, critic weights) far from the cause — FIX: validate/coerce per-field (e.g. only accept values matching the dataclass field type, else keep default).

- [P2] maxbridge/config.py:15-22 — `os.makedirs` runs at IMPORT time — TRIGGER: profile dir unwritable, or a FILE named `MaxGaffer` exists in `%LOCALAPPDATA%` → `FileExistsError`/`PermissionError` raises during `import config`, breaking the whole plugin load (and contradicting the module's "importable off-Max" contract on odd environments) — FIX: defer directory creation to first save/sessions_dir() call, or wrap makedirs in try/except.

- [P1] maxgaffer/maxbridge/config.py:69 — config values applied without type validation, then crash UI slots — TRIGGER: hand-edited/version-skewed config.json with `"max_iterations": "five"`, `"loop_width": 480.0` or `null` — EVIDENCE: `setattr(cfg, k, v)` is blind; dock.py:777 computes `int(self.cfg.max_iterations)` BEFORE `_start_match_all`'s try (ValueError escapes the slot), and SettingsDialog `setValue()` (dock.py:1154/1166-1169) TypeErrors on wrong types so Settings won't even open — FIX: per-field type coercion with defaults in `load()`; move the `est` math inside the try.

- [P2] maxgaffer/maxbridge/config.py:67 — non-dict config.json blocks launch entirely — TRIGGER: config.json containing `[]` or `"x"` — EVIDENCE: `d.items()` raises AttributeError, not covered by `except (OSError, ValueError)`; escapes `load()` → dock `__init__` (dock.py:107) → bootstrap messageBox "failed to launch: AttributeError: 'list' object has no attribute 'items'" — plugin dead until the file is manually deleted — FIX: `if not isinstance(d, dict): return cfg`.