- [P0] maxgaffer/core/png_min.py:60 — unbounded `zlib.decompress` before any dimension/size sanity check — TRIGGER: in stock Max (no Pillow), artist binds a hostile "reference.png" that is a decompression bomb (e.g. a ~30 MB file of zeros that inflates to 30 GB at a declared 100000×100000 RGB — dimensions alone are legitimately declared, payload is real) → ANALYZE/seed/match calls `controller.ref_stats` → `metrics.compute_stats` → `read_png_rgb` on Max's main thread — EVIDENCE: `raw = zlib.decompress(bytes(idat))` runs with no `max_length` and no cap on `width`/`height`/expected `height*(stride+1)`; the only size check (`len(raw) < height*(stride+1)`, line 64) happens AFTER the full allocation, so the process OOMs/thrashes/freezes before any guard fires — FIX: reject `width`/`height` above a sane ceiling (e.g. 32768) and/or expected raw size (e.g. 256 MB) before decompressing, and use `zlib.decompressobj().decompress(data, max_length)` so oversized streams fail fast instead of allocating.

- [P1] maxgaffer/core/png_min.py:47-49 — uncaught `struct.error` on malformed IHDR chunk — TRIGGER: a PNG file with valid signature but an IHDR chunk whose declared length ≠ 13 (short, overlong, or length-field-huge-but-truncated file); reproduced live: SIG + length=12 + "IHDR" → `struct.error: unpack requires a buffer of 13 bytes` propagates out of `read_png_rgb`, through `metrics.compute_stats` (which has no try/except around the png_min call, metrics.py:40), up through `controller.ref_stats`/`stats_for` and `director.run_match`'s unguarded `hooks.stats(path)` (director.py:179,408,504) — EVIDENCE: the module contract is "anything [unsupported] returns None" and SPEC §2 locks "loop stats can never fail"; UI catch-alls (dock.py `_Worker.run`, `_seed_dome`) prevent a Max crash, but the entire match/seed run aborts with a cryptic struct error instead of the designed graceful fall-through to Pillow/sidecar/Max-transcode — FIX: wrap the IHDR `struct.unpack` in `try/except struct.error: return None` (or check `length == 13` before unpacking).

- [P1] maxgaffer/core/hdr_min.py:26-41 (+ write_hdr:101-107) — `float_to_rgbe` exponent byte overflows 255 for finite inputs ≥ ~1.7e38 → `write_hdr` raises uncaught `ValueError` — TRIGGER: reproduced live: `float_to_rgbe(2e38, 0, 0)` returns `(150, 0, 0, 256)` and `write_hdr(path, [[(2e38, 1.0, 1.0)]])` raises `ValueError: bytes must be in range(0, 256)` from the `bytes(...)` calls at lines 105/107 — EVIDENCE: NaN/inf/negatives are sanitized (line 29-31) and the low end is guarded by `m < 1e-9`, but the high end is not: any finite `m ≥ 2^127` gives `exp + 128 ≥ 256`; `write_hdr` catches only `OSError`, violating its documented "False on failure" quiet-degrade contract. Not reachable via the shipped controller path (seed values ≤ ~10³; `seed_dome` never passes `sun_strength`), but `write_hdr`/`build_seed` are public core APIs and `sun_strength` is a public parameter — FIX: clamp `m` to the max RGBE-representable value (255·2¹¹⁹ ≈ 1.69e38) in `float_to_rgbe`, or catch `ValueError` in `write_hdr` and return False.

- [P2] maxgaffer/core/hdr_min.py:194-203 — old-style RLE .hdr in the 8..32767 width band is silently misdecoded as flat scanlines instead of returning None — TRIGGER: read a Radiance file written by an old-style-RLE encoder (width ≥ 8, no `2,2,w>>8,w` marker) — EVIDENCE: docstring (line 11) promises "anything exotic (old-style RLE, XYZE) returns None", but the flat fallback consumes `width*4` raw bytes per scanline and produces garbage floats with no error; currently latent because `read_hdr` is used only in tests/on-box spikes, not production — FIX: either implement old-style RLE decode or tighten the docstring; at minimum, when width is in the RLE band and the marker is absent, treat as unsupported unless the remaining byte count exactly equals `height*width*4`.

- [P2] maxgaffer/core/png_min.py:69 — `max_dim=0` → uncaught `ZeroDivisionError` — TRIGGER: reproduced live: `read_png_rgb(p, max_dim=0)` raises at `max(width, height) // max_dim` — EVIDENCE: all internal callers pass fixed defaults (160/256/384), so unreachable today, one caller-refactor away — FIX: `step = max(1, max(width, height) // max(1, max_dim))`.

- [P2] maxgaffer/core/hdr_min.py:41 — write_hdr raises ValueError instead of returning False for finite values ≥ 2^127 — TRIGGER: any caller passing rows containing m ≥ ~1.7e38 (e.g. a future caller passing sun_strength=1e40 to build_seed; unreachable today since normalize bounds pixels to ~3.5e5 and strength defaults to 200) — EVIDENCE: exp+128 > 255 makes `bytes(...)` raise ValueError at lines 103-107, and write_hdr only catches OSError (line 109), violating its "False on failure" contract — FIX: clamp the exponent (`if exp + 128 > 255: return (255, 255, 255, 255)`).

- [P1] maxgaffer/sidecar/metrics_cli.py:47 — One corrupt image kills the whole CLI invocation (and the in-process stats path raises instead of degrading) — TRIGGER: truncated/corrupt PNG (sig + declared IHDR length but short chunk) → `png_min` `struct.unpack(">IIBBBBB", chunk)` raises `struct.error` (png_min.py:48, unguarded); Pillow doesn't save it — its failure falls through to `png_min` → `metrics.compute_stats` RAISES instead of returning None — EVIDENCE: verified `struct.error: unpack requires a buffer of 13 bytes` from `compute_stats`; in the CLI there is no per-image guard, so stdout gets no JSON (contract says "null for unreadable") and every other image in the batch is lost; in-process, `Controller.ref_stats` (controller.py:161) raises on a corrupt PNG reference, so `run_match` dies at controller.py:435 and the designed "stats unavailable → LLM-visual mode" fallback (controller.py:436-438) is unreachable — FIX: guard `compute_stats` inside metrics_cli per image (`try/except Exception → stats: null`), and catch `struct.error`/exceptions in `png_min.read_png_rgb` (or in `metrics.compute_stats`) returning None.