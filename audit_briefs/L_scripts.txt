- [P2] scripts/install.bat:35 — no `errorlevel` check after the repo_path recording step — TRIGGER: pre-existing `%LOCALAPPDATA%\MaxGaffer\config.json` containing corrupt JSON (truncated by an earlier crash) — EVIDENCE: `json.load(open(p))` raises inside `python -c`, the step exits non-zero silently, repo_path is never recorded, and the script still prints "=== done ==="; the only fallback is the `setx MAXGAFFER` on line 36, which is itself `>nul` + unchecked — FIX: add `if errorlevel 1 echo [!] could not record repo path` and make the python one-liner tolerate corrupt JSON (`try/except ValueError`).

- [P2] scripts/sim_match.py:153-155 vs docstring lines 11-13 — asserted pass criteria contradict the documented criteria — TRIGGER: reading the docstring as the contract (EV ≤0.75 stop, WB ≤800 K) — EVIDENCE: code asserts the stricter `<= 0.5` stop and `<= 700.0` K; a run landing at 0.6 stop / 750 K FAILs while the doc says PASS — FIX: align one with the other (code is stricter; update the docstring).

- [P2] scripts/sim_match.py:58 — hard Pillow dependency, undocumented — TRIGGER: `python scripts/sim_match.py` on any python without Pillow (the SPEC §2 "stdlib floor" ethos implies the proof runs anywhere) — EVIDENCE: `World.render` does `from PIL import Image, ImageDraw`; the module docstring (line 9) never mentions it, unlike live_gateway_smoke.py:53 which does — first render dies with a raw ImportError — FIX: document the requirement in the docstring, or emit a friendly "pip install pillow" message on ImportError.

- [P2] scripts/live_gateway_smoke.py:99 vs docstring line 5 — "The key is never printed" but the key IS partially printed — TRIGGER: normal run; output lands in CI logs / pasted reports — EVIDENCE: `print(f"key: {key[:6]}…{key[-4:]} …")` exposes 10 characters of the credential, directly contradicting the documented guarantee — FIX: print a fixed label ("key: oc_…(redacted)") or a hash prefix only.

- [P2] scripts/onbox_spikes.py:171-175 — EV-direction spike false-FAIL at the genome clamp — TRIGGER: a scene whose exposure host reads EV ≥ 18 (genome max is 20.0, genome.py:56) — EVIDENCE: `st.set("exposure.ev", base_ev + 2.0)` clamps to 20.0, the +2 EV probe render is identical to base, and the check raises "INVERTED: key rose…" on a perfectly healthy box — FIX: probe `base_ev - 2.0` (or pick the direction that stays inside bounds) and compare.

- [P2] scripts/sim_match.py:231 — uncaught `ValueError` from `consolidate_analyses([])` (and uncaught `OmegaError` at line 222) — TRIGGER: live mode with the gateway returning non-JSON on all 3 ANALYZE samples (validate_analysis raises ParseError → every sample skipped → `samples=[]`), or a 401/network failure mid-Phase-B — EVIDENCE: `consolidate_analyses` raises `ValueError("no analysis samples")` (consensus.py:40-41) and `omega.call` raises `OmegaError`; neither is caught in Phase B, so the script dies with a raw traceback after Phases A/C passed, and no OVERALL verdict prints — FIX: wrap the sampling loop for `omega.OmegaError` and fall back to `scripted.analyze()` when `samples` is empty, matching the script's own "falls back if offline" contract.

- [P1] scripts/preflight.py:98-106 — preflight demands `vantage_console.exe` on stock Vantage 3.x — TRIGGER: run preflight on any box with a stock (non-Developer-Edition) Vantage 3.x install — EVIDENCE: `_vantage()` unconditionally checks `cfgmod.load().vantage_console` and reports `[!!] not found`, yet SPEC §2 locked decisions + config.py:33 (`final_render_backend="vray"` default) say stock 3.x has no render CLI and it is not needed; onbox_spikes.py:293-301 was fixed to check `vantage_exe` and only probe `vantage_console` when `backend=="vantage_cli"`, but preflight was not — a clean, supported box reads as NOT ready — FIX: mirror the spike logic: check `cfg.vantage_exe` for handoff, probe `vantage_console` only when `final_render_backend=="vantage_cli"`.

- [P2] scripts/install.bat:5 — `EnableDelayedExpansion` mangles repo paths containing `!` — TRIGGER: clone into a folder whose name contains `!` (legal NTFS, e.g. `D:\Dropbox!\3DSL`), run installer — EVIDENCE: with delayed expansion on, `!` introduced by `%REPO%` expansion is re-scanned at use sites, so the mangled path is written to config.json (line 35) and to the MAXGAFFER env var (line 36); startup/maxgaffer_startup.py:13-24 then can't find the repo and the plugin silently never loads — FIX: `setlocal DisableDelayedExpansion` (nothing in the script needs it) or use `setx MAXGAFFER "%~fI"`-style FOR-var output directly.

- [P2] scripts/onbox_spikes.py:283 — scene-restore `apply_state` in the `finally` is itself unguarded — TRIGGER: anything that makes the restore apply fail mid-spike (user deletes the sun node during probing, V-Ray exposure control removed, undo-system error) — EVIDENCE: the exception propagates out of `main()`, the spike report (lines 320-333) is never written, and the scene is left partially restored; the undo record itself stays balanced (apply.py:110 uses a `with pymxs.undo` context manager) — FIX: wrap the restore in `try/except Exception` and log a loud "MANUAL RESTORE NEEDED" line before the report section.