- [P2] SPEC.md:6 / SPEC.md:68 / README.md:273 — test-count claims are all stale — TRIGGER: any reader trusting the docs — EVIDENCE: SPEC header says "114 tests", SPEC §3 says "77 pytest", README says "~70 tests"; the suite currently collects 163 test functions with **148 passed + 1 skipped** (verified this turn: `pytest tests/ -q`) — FIX: update all three to the real number or drop the counts.

- [P2] README.md:36 vs SPEC.md:6 / README.md:124 / README.md:191 — three mutually inconsistent "live Phase B" headline numbers — TRIGGER: a reader comparing the intro table (90.0 target_reached), the deep-match section (98.25, 4° error), and the verification section (85.1 best run) — EVIDENCE: only the 85.1 is attributed to a dated run (2026-07-16); the 90.0 and 98.25 claims carry no run date and contradict each other as "the" measured live result — FIX: keep one dated number per benchmark phase and label each with its run date.

- [P2] SPEC.md:43-68 — §3 architecture tree omits 7 core modules and 2 bridge modules — TRIGGER: using SPEC §3 as the module map — EVIDENCE: core list lacks consensus.py, feedback.py, planner.py, scenedigest.py, scenarios.py, domeseed.py, hdr_min.py; maxbridge list lacks digest.py and execute.py — all present on disk and imported by scripts in this audit — FIX: regenerate the tree.

- [P2] SPEC.md:6 vs pyproject.toml:3 — version drift — TRIGGER: reading SPEC's "v0.8.0 CONVERSATION" status line — EVIDENCE: pyproject declares 0.9.3 and the repo ships v0.9 features (dome seed, scenario board) that SPEC §10 describes as an addendum — FIX: bump the SPEC status header.