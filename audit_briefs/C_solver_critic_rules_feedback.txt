- [P2] maxgaffer/core/solver.py:40-41,59-63 — solver raises uncaught TypeError/IndexError on malformed stats dicts — TRIGGER: stats from the Pillow sidecar (`Controller._sidecar_stats`, controller.py:141-143 validates only the envelope `[{"stats": dict}]`, never field shapes) returning e.g. `{"log_key": null}` or `{"lab_mean": null}` / a 2-element `lab_mean` — EVIDENCE: runtime-verified `solve_ev({'log_key': None}, ...)` → `TypeError: float() argument must be...`; `solve_wb({'lab_mean': None}, ...)` → `TypeError: 'NoneType' object is not subscriptable`; short list → `IndexError`; `analytic_pass` is called unguarded at director.py:245, so one bad stats dict aborts the entire match run — FIX: coerce with `float(... or 0.0)`-style guards and shape-check `lab_mean`/`lab_mean_hi` (require `isinstance(x,(list,tuple)) and len(x)==3` before indexing), or treat malformed stats as "metrics unavailable" (the designed degradation path).

- [P2] maxgaffer/core/solver.py:40-42,71-74 — NaN in stats produces a spurious max-step EV/WB move instead of a no-op — TRIGGER: any NaN in `log_key`/`lab_mean` b* (e.g. NaN emitted by the sidecar's Pillow path on a degenerate image; `json.dumps` writes `NaN`, `json.loads` reads it back) — EVIDENCE: runtime-verified `solve_wb` with NaN b* returns 8000 K from 6500 K (deadband `abs(nan) < x` is False, `min(1500, nan)` → +1500 K); `solve_ev` with `log_key=nan` floors to `1e-5` (treated as a near-black frame) and fires a full ±2.5-stop correction — FIX: `math.isfinite` checks on `key_ref/key_cur/b_ref/b_cur`; return `None` (skip the iteration's analytic move) when non-finite.

- [P2] maxgaffer/core/solver.py:65-70 — WB highlight/full-mean mixing guard is presence-based and shape-blind — TRIGGER: one stats dict carries `lab_mean_hi: null` (or a malformed value) while the other's is valid; or a stats dict missing `lab_mean` entirely — EVIDENCE: runtime-verified `solve_wb({'lab_mean_hi': None, 'lab_mean':[50,0,5]}, {'lab_mean_hi':[50,0,20], ...})` returns 5150 K — it compared ref's *full* mean against cur's *highlight* mean, exactly the cross-quantity comparison the code comment forbids; a missing `lab_mean` silently defaults b* to `0.0`, manufacturing a large WB move from absent data — FIX: compute `use_hi` from *validity* of both sides' `lab_mean_hi` (shape-checked), and abstain (return None) when the chosen source is missing on either side.

- [P2] maxgaffer/core/solver.py:16,53 — docstring promises a "config-tunable" kelvin-per-b* slope but no config path exists — TRIGGER: user/on-box checklist tries to tune the WB slope after the #6 visual sign check — EVIDENCE: `kelvin_per_b` appears only in solver.py; `analytic_pass` (the only in-loop caller) never passes it and `MatchConfig` has no such field, so the slope is hard-wired to 90 K/unit — FIX: add `wb_kelvin_per_b` to `MatchConfig`/app config and thread it through `analytic_pass`, or correct the docstring.

- [P1] maxgaffer/core/critic.py:60 — ill-typed `critic_weights` from config.json crashes every score with AttributeError/ValueError on Max's main thread — TRIGGER: user hand-edits `%LOCALAPPDATA%\MaxGaffer\config.json` `"critic_weights"` to a well-formed-but-wrong-typed value (`"junk"`, `[0.2,…]`, `{"key":"lots"}`); `config.load()` does a blind `setattr` (config.py:67-69), then `critic.score` runs `weights.items()` / `float(v)` unguarded; tasks/plan.md:118 explicitly tells users to edit `config.critic_weights`, so this input is expected — EVIDENCE: verified — `critic.score(ref,ref,"junk")` → `AttributeError: 'str' object has no attribute 'items'`; `{"key":"lots"}` → `ValueError`; the raise escapes `director.run_match` (only ParseError is caught there), aborting the run before the land-on-best re-apply, and in `run_scenarios` it skips the scene-restore at controller.py:708, leaving a candidate's light applied; negative/mixed weights also pass (`{"key":-20.0}` accepted), producing scores outside 0..100 that corrupt target/stall logic — FIX: validate weights at config load (dict, known keys, finite ≥0 floats, else fall back to defaults) AND coerce defensively in `critic.score` (`try: w.update(...)` skipping non-finite/negative values); put the board's restore-apply in `try/finally`.

- [P2] maxgaffer/core/rules.py:82 — `float(semantics.get(...))` raises on un-coerced semantics loaded from the human-editable sidecar — TRIGGER: hand-edit `<scene>.maxgaffer.json` semantics to `"sun_bearing_deg": "high"` or `"wb_kelvin_estimate": null` (SPEC §2 advertises the sidecar as human-readable/diff-able, and `analyze_reference` returns the cached dict verbatim, bypassing `validate_analysis`); next MATCH or BOARD dies at first-guess — EVIDENCE: verified — `initial_state({"sun_bearing_deg":"high",...})` → `ValueError: could not convert string to float: 'high'` (line 107 has the same flaw for `wb_kelvin_estimate`); dock catches it, but the user gets an opaque "✗ unexpected" with no hint the sidecar is the cause — FIX: re-validate cached semantics through `parse.validate_analysis`-style coercion on `Session.load`, or use `_num`-style try/except coercion in `rules.initial_state`.

- [P2] maxgaffer/core/critic.py:76 — achromatic-vs-achromatic pairs get full hue credit instead of renormalizing the component out — TRIGGER: two near-neutral images (all-black ref vs all-white render, gray overcast both sides) — both `hue_hist`s are all-zero, `cosine` returns 1.0 for two empty vectors — EVIDENCE: verified — black-vs-white scores 15.29 with `hue=1.0` while key/envelope/histogram/color are all 0.0; the direction component is *omitted* when grids carry no signal, but hue awards maximum marks for "no hue information", inflating scores on exactly the degenerate pairs the hunt list asks about — FIX: skip the hue component (renormalize) when either side's `hue_hist` sums to ~0, mirroring the direction guard at line 84.

- [P2] maxgaffer/core/feedback.py:22 — stem matching ignores negation, applying wrong-direction scene nudges — TRIGGER: a natural note like "it's not too bright" or "shadows aren't too soft" — the regex matches "too bright" and applies +0.7 EV (darker), the opposite of intent — EVIDENCE: `nudges_from_note("not too bright", keys, [])` returns `{"exposure.ev": 0.7}`; the nudge is baked into `state0`, the base of ALL three lens branches (controller.py:560-602), so it contaminates the whole ensemble, not just one branch (the following deep match's analytic solver re-fixes EV, which bounds the damage to solver-owned params; geometry/WB-note misreads persist longer) — FIX: cheap negation guard — skip a rule when a negator (`not|n't|no`) appears in the same local clause window already scanned for intensifiers (lines 53-56).

- [P2] maxgaffer/core/feedback.py:77 — multiplicative group nudge on a zeroed group is a silent no-op — TRIGGER: rules zeroed `group.practicals` for a daylight reference (`initial_state` line 122), then the user types "more practicals" / "practicals too dim" — EVIDENCE: verified — `cur=0.0 → max(0.0, 0.0 * 2**0.5) = 0.0`; `apply_changes` accepts it, so the refine log prints "note → group.practicals = 0.00 (instant)" as if it worked while nothing changed (log-space math can never leave 0 multiplicatively) — FIX: bootstrap zero baselines to a floor before scaling (e.g. `cur if cur > 1e-3 else 0.25`), or fall back to an additive step when `cur == 0`.

- [P2] maxgaffer/core/critic.py:62 — no shape guards on present-but-mistyped stats fields; only *missing* keys are safe — TRIGGER: a stats dict with `"log_key": None`, `"lab_mean": [0,0]` (short), or `"lab_mean": None` — reachable via the sidecar trust boundary (`controller._sidecar_stats` returns an unvalidated dict from an external system_python; version-skewed or edited `metrics_cli` output) — EVIDENCE: verified — each raises TypeError/IndexError out of `critic.score` on the main thread mid-loop, aborting the match before land-on-best; the existing test (`test_missing_fields_do_not_crash`) pins only the `{}` case — FIX: normalize stats at the boundary (validate `log_key` finite, `lab_mean` length-3 numeric, `p` numeric) in `_sidecar_stats`, or wrap component reads with safe coercions like `_sub` does for structure.