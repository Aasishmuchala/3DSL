"""Pure planning/matching brain — ZERO pymxs, ZERO Qt, stdlib-only hard floor.

Enforced by tests/test_no_pymxs.py so the entire matching engine runs and is tested on any
OS (this repo is developed on macOS; Max only ever sees the maxbridge/ui layers).
"""
