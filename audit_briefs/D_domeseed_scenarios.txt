- [P1] maxgaffer/core/scenarios.py:99 — unvalidated cached semantics let NaN/Infinity reach a scene write on Max's main thread — TRIGGER: hand-edited/corrupted `<scene>.maxgaffer.json` containing `"semantics": {"sun_bearing_deg": NaN}` (Python's json.load accepts NaN/Infinity literals by default, session.py:114; semantics stored unvalidated, session.py:68; cache returned raw, controller.py:230-231) → run scenario board — EVIDENCE: build_scenarios merges the dict unvalidated (line 99) → rules.initial_state `float(...)` = NaN → genome.clamp wrap path `math.fmod(nan, 360.0)` returns nan (genome.py:75-77) → state.values["sun.azimuth_deg"] = NaN → run_scenarios applies it via write_sun_angles (apply.py:126-128), which has no per-parameter try/except, so a NaN transform is written to the sun node or a pymxs exception aborts mid-board (see next finding). Only the fresh-LLM path is safe (parse._num clamps NaN to lo) — FIX: run cached semantics through parse-style clamps on session load, and make genome.clamp map non-finite values to spec.lo.

- [P2] maxgaffer/core/domeseed.py:330 — .hdr write failure is reported as a reference-read failure — TRIGGER: read-only run dir, disk full, or over-long path during seeding — EVIDENCE: build_seed returns None identically for load failure (line 298) and write_hdr failure (330-331), so controller.py:770-771 raises "could not read the reference for seeding", misdirecting the artist when the reference read fine — FIX: distinguish the two (raise a typed error or include the failure stage in the returned meta).

- [P2] maxgaffer/core/domeseed.py:297 — the external-pano upgrade seam cannot ingest HDR files — TRIGGER: build_seed(pano_path="sky.hdr" / ".exr") as SPEC §10's DiffusionLight-class gate promises — EVIDENCE: pano_path routes through metrics._load_pixels (8-bit sRGB PNG/JPEG only; Pillow has no RGBE reader, png_min is PNG-only) so a real equirect HDR returns None, and even LDR panos are crushed to 8-bit before sun injection; hdr_min.read_hdr exists for exactly this re-ingestion but is never called (test-only today), and no production caller passes pano_path at all — FIX: sniff the extension and route .hdr through hdr_min.read_hdr before falling back to _load_pixels.