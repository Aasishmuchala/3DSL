- [P1] maxgaffer/maxbridge/vantage.py:172 + 118-124 — stale file from a previous batch is accepted as this run's render — TRIGGER: run the `vantage_cli` backend twice into the same out_dir; on run 2, vantage_console exits 0 but writes nothing (license bounce, headless failure); a pre-existing `camA.png` (or any same-stem sibling — `Shot10.png` satisfies `startswith("Shot1")`) sits in the folder — EVIDENCE: `render_stills` marks the camera `"ok"` when `proc.returncode == 0 and _output_written(...)`; `_output_written` accepts any non-empty file sharing the stem in that folder, with no mtime/freshness check — FIX: delete/renaming-guard the expected output before spawning, and require mtime ≥ spawn time (or render into a fresh per-batch dir).

- [P1] maxgaffer/maxbridge/vantage.py:171 — no cancellation and a 60-minute hang budget per job on the batch path — TRIGGER: user runs final renders with `final_render_backend:"vantage_cli"`; vantage_console hangs (dead license server, modal on a hidden desktop); the UI's Cancel button is never enabled during finals (dock.py:912-941 enables `btn_cancel` nowhere, unlike the match flows) and no `should_cancel` reaches `render_stills` — EVIDENCE: `subprocess.run(..., timeout=60*60)` per job, sequential; Max stays responsive but the batch is un-killable short of terminating 3ds Max (unsaved-scene loss); the subprocess is a zombie-in-waiting the user cannot reap — FIX: accept a `should_cancel`/Popen handle, poll with short `communicate` timeouts and `kill()` on cancel; enable Cancel during finals; lower per-frame timeout.

- [P1] maxgaffer/maxbridge/vantage.py:40-62 — live-link "start" blindly fires a TOGGLE and the UI reports success as "started" — TRIGGER: user already has a live link running (started from the V-Ray menu, as the code's own fallback message recommends), then clicks MaxGaffer's live-link button — EVIDENCE: `start_live_link` executes the first global/action found with no state probe; the docstring itself states the same action "TOGGLES the link off"; dock.py:904 then displays `link: started — …` while the link was just killed — FIX: probe link state before executing (or relabel UI/report as "toggle"), and never claim "started" without a state read.

- [P2] maxgaffer/maxbridge/vantage.py:180,185 — one failed job silently abandons the rest of the batch — TRIGGER: job 2 of 8 fails (or errors) in a `vantage_cli` batch — EVIDENCE: `break` exits the loop; cameras 3-8 get no result entry and the caller (dock.py:936) only logs present keys, so the overnight batch quietly renders 1/8; the rc==0-but-no-output case also reports the misleading text `vantage exit 0` (line 177) — FIX: `continue` after recording the failure, or surface the skipped cameras explicitly.

- [P2] maxgaffer/maxbridge/render.py:57-59 — bitmap leaked when `rt.save(bm)` throws — TRIGGER: disk full / unwritable path / codec failure on save after a successful render — EVIDENCE: the outer `except` returns None but `rt.close(bm)` is skipped (only the success path closes it); the bitmap framebuffer leaks to MAXScript GC — FIX: close `bm` in the `finally` block.

- [P2] maxgaffer/maxbridge/render.py:60 — success is judged by file existence, so a stale output can pass — TRIGGER: `render_finals_vray` into a user-chosen folder that already contains `camA.png` from an earlier run; `rt.save(bm)` fails silently (no MAXScript exception, no bytes written) — EVIDENCE: `return out_path if os.path.exists(out_path)` — the old file satisfies the check and the final is reported "ok" with yesterday's pixels — FIX: delete a pre-existing target before rendering, or compare mtime/size against pre-render values.

- [P2] maxgaffer/maxbridge/execute.py:141-149, 161-168 — created-light window between ctor and layer-add is not fault-isolated; sun target leaks off-layer — TRIGGER: any exception at `node.name = op["name"]` or the `op["placement"]` access (today reachable only via an unvalidated caller, e.g. a future API path bypassing `validate_plan`); or `node.target = tgt` failing for a VRaySun — EVIDENCE: the `rt.VRayLight()`/`rt.Targetobject()` constructors create scene nodes immediately; a mid-sequence failure leaves the node in the scene, off the `MG_lights` layer and absent from `report["created"]` (the sun-target case is guarded but leaks the named helper off-layer) — FIX: wrap the post-creation sequence so a failure deletes the node (`rt.delete(node)`), and add the target to `MG_lights` too.

- [P1] maxgaffer/maxbridge/execute.py:51-52 (via 128) — string "false" is coerced to `True` on bool properties — TRIGGER: LLM plan op `{"op":"set","target":"node:MG_key","prop":"enabled","value":"false"}` — passes `_valid_value` (any str < 500 chars is "valid", planner.py:87) — EVIDENCE: `_coerce` hits `isinstance(current, bool) or isinstance(value, bool) → bool(value)`, and `bool("false") is True`; the light is turned ON, the report logs a benign-looking `True → True` — FIX: in the bool branch, map `"true"/"false"` (case-folded) strings explicitly and reject other strings.

- [P2] maxgaffer/maxbridge/execute.py:128 + core/planner.py:85 — NaN/±inf floats pass plan validation and are written to scene properties — TRIGGER: LLM emits `{"prop":"multiplier","value":NaN}` (Python's `json.loads` accepts bare `NaN`/`Infinity`, omega.py:66) — EVIDENCE: `_valid_value` accepts any `int/float` with no `math.isfinite` check; `_coerce` passes it through; `setattr` writes NaN into a live light, poisoning renders until reverted (it is inside the undo record, so recoverable) — FIX: reject non-finite numbers in `_valid_value`.

- [P2] maxgaffer/maxbridge/vantage.py:97 — batch export leaves the viewport parked on the last exported camera — TRIGGER: `export_and_open_vantage` over 5 cameras — EVIDENCE: `export_vrscene` calls `sc.set_active_camera(camera_name)` (`viewport.setCamera`) and never restores the previously active camera; controller.py:893 documents the *lighting* state leak but not the viewport hijack — FIX: capture the active viewport camera before the loop and restore it after.