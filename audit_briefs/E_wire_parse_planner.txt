- [P1] maxgaffer/core/omega.py:36-38,135 — `extract_text` raises TypeError on a malformed 200 payload, escaping `call`'s typed-error contract — TRIGGER: gateway (or a proxy/CF error page that parses as JSON) returns 200 with a content block like `{"type":"text","text":null}` or a non-string `text` — EVIDENCE: `b.get("text", "")` returns `None` when the key exists with value null, so `"\n".join(...)` throws TypeError at omega.py:135, which is NOT inside any try; `call` thus raises a raw TypeError instead of `OmegaError`. Through `ctrl.io` it is stringified into RuntimeError and contained by broad UI catches, but `SettingsDialog._test` catches ONLY `OmegaError` (dock.py:1197) → unhandled exception in a Qt slot on Max's main thread — FIX: coerce with `str(b.get("text") or "")` in `extract_text`, or wrap the `extract_text` call so any failure becomes `OmegaError(kind="other")`.

- [P2] maxgaffer/core/omega.py:8,22-23 — retry policy: fixed backoff with no jitter, and the "~120s wall-clock ceiling per attempt" docstring is false — TRIGGER: `urlopen(timeout=120)` is per socket I/O operation, not per request; a slow-drip server can hold one attempt many times longer — EVIDENCE: BACKOFF_S = (2,6,15) fixed, `time.sleep` at omega.py:148; docstring line 8 claims a wall-clock ceiling that nothing enforces — FIX: add small random jitter to sleeps and either fix the docstring or wrap the attempt in an overall deadline.

- [P2] maxgaffer/core/omega.py:160-174 — `image_block_from_file` has no size guard in core — TRIGGER: any caller other than `controller._image_block` (which caps at 3.5 MB, controller.py:213) — e.g. scripts/API reuse — feeding a large PNG/JPEG — EVIDENCE: the file is read fully into memory and base64-inflated ×4/3; the 3.5 MB guard lives in maxbridge, not in this core function; Anthropic's 5 MB image limit leaves only ~7% headroom even on the guarded path — FIX: enforce/accept a `max_bytes` parameter inside `image_block_from_file` itself.

- [P2] maxgaffer/core/planner.py:85 — `_valid_value` accepts NaN/Infinity, so non-finite numbers flow into raw scene-property writes — TRIGGER: LLM plan reply containing `"value": NaN` or `Infinity` (Python `json.loads` accepts these literals; `parse_json_from_text` uses defaults), or `[NaN, r, g]` colors — EVIDENCE: verified — `validate_plan` accepts `{"prop":"turbidity","value":NaN}` with zero rejections; `_coerce` passes float NaN through to `setattr` in execute.py, which fault-isolates *raised* errors but a silent NaN write into a V-Ray float property succeeds and poisons renders — FIX: add `math.isfinite(v)` checks for all numeric branches in `_valid_value` (and reject non-finite list elements).

- [P2] maxgaffer/core/parse.py:79 — `validate_deltas` accepts NaN/Infinity change values — TRIGGER: LLM replies with `{"param":"sun.azimuth_deg","value":NaN}` or `"value":1e999` — Python's `json.loads` parses bare `NaN`/`Infinity` tokens and `float()` accepts both, so they enter `changes` — EVIDENCE: genome's `limit_step` then maps NaN to a FULL positive max-step move (`min(step, nan)` → step by comparison semantics) instead of rejecting; the scene takes an unrequested bounded move that the critic must later revert. NaN-in-numeric-code already caused one real regression in this repo (test_domeseed.py:206) — FIX: `import math; if not math.isfinite(value): continue` in the `validate_deltas` item loop (and optionally in `_num`).