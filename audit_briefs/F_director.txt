- [P1] maxgaffer/core/director.py:170 — **hook exception mid-loop abandons the keep-best guarantee** — TRIGGER: any hook raising after iter0 has applied an exploratory state — e.g. `stats_for` choking on a corrupt 480px PNG (line 179), a pymxs error inside `apply_state` (line 170), or `OmegaError` after gateway retries (line 296, only `ParseError` is caught at 297) — EVIDENCE: there is no try/finally around the iteration body; the "best state ALWAYS re-applied at the end" apply (lines 347–351) is skipped, and the dock (dock.py:758–761, main thread) just logs "✗ unexpected" without restoring. Verified empirically: exception at iter1 left the scene at altitude 36.25° while the tracked best was 30.0°. The pre-match snapshot exists but the user is never told to Restore — SPEC §7 "Match ruins a good rig → snapshot + Restore" assumes the loop lands cleanly. FIX: wrap the loop body in try/finally that re-applies `best_state` (or `start_state`) before re-raising.

- [P1] maxgaffer/core/director.py:436 — **polish log-step math makes `dome.intensity = 0.0` a dead axis** — TRIGGER: deep match starting from a state where the LLM drove `dome.intensity` to 0.0 (genome allows it: lo bound is 0.0, unlike sun.intensity's 0.05) — EVIDENCE: `v * (2.0 ** (direction * stride))` with v=0 yields 0, so line 438's `abs(cand.get(key) - v) < 1e-6` fires the "clamped to a bound" break every round; verified: polish with a monotonically dome-favoring score function left dome.intensity at 0.0 with zero gain — the dome can never be turned back up by the deep-match finisher. FIX: for log-scale params with v ≤ 0, seed the probe from the fine floor (e.g. `cand.set(key, max(v, floor) * 2**±stride)`) or fall back to an additive nudge.

- [P2] maxgaffer/core/director.py:497 — **failed sweep leaves the scene at the last probed azimuth** — TRIGGER: `run_sun_sweep` exits via "not enough renders" (line 511), unusable reply (line 516), or an exception from `hooks.stats`/`llm_pick` — EVIDENCE: `probe.set` + `hooks.apply(probe)` happen *before* the render, and no exit path restores the incoming azimuth; verified: a sweep returning `None` left the scene at azimuth 180° when the start was 100°. Normally masked by the loop's iter0 apply, but on the gateway-down exception path (common) the match aborts with a random sun direction and only manual Restore as recovery. FIX: snapshot `state.get("sun.azimuth_deg")` on entry and re-apply it (or the winner) before every return, in try/finally.

- [P2] maxgaffer/core/director.py:361 — **polish runs after a `render_failed` stop, burning renders against a dead renderer** — TRIGGER: render hook starts failing at iter ≥ 1 with best_score < 99.5 and `polish=True` — EVIDENCE: the gate excludes only `"cancelled"`; verified 36 failed polish render attempts (4 rounds × 9 params) with gain 0.0 after the loop itself stopped for render failure — up to ~90 with default `polish_rounds=10`, each a potential V-Ray error dialog on the main thread. FIX: add `stop_reason != "render_failed"` to the polish gate.

- [P2] maxgaffer/core/director.py:369 — **redundant final apply duplicates an undo record** — TRIGGER: every polished run — EVIDENCE: all four `run_polish` return paths already `hooks.apply(best)` (lines 421, 457, 463, 467), then line 369 applies the identical state again; with one undo record per `apply_state` (apply.py:110) the user gets a trailing no-op Ctrl+Z step. Same duplication in the classic loop when the last scored state is already the applied one. FIX: drop the line-369 apply (polish already landed) or no-op-detection in apply.

- [P2] maxgaffer/core/director.py:248 — **leash-hit accounting misses genome-bound saturation, killing the SPEC-mandated albedo diagnosis** — TRIGGER: start EV near a genome edge (e.g. EV 19) with an albedo-trap reference wanting EV beyond the bound — EVIDENCE: `solve_ev` pre-clamps to [-4, 20], so `|leashed - analytic| < 1e-6` when the leash window [15, 23] is wider than the genome bound; no hit is ever counted, the "≥2 hits → albedo diagnosis" (line 341, SPEC §2 analytic leash) never fires, and the run just reports "stalled". FIX: count a hit when the solver's pre-clamp intent (or repeated boundary saturation) exceeds what was applied.

- [P2] maxgaffer/core/director.py:432 — **polish climb loop doesn't poll `should_cancel`** — TRIGGER: user hits Cancel during a long accelerating climb on one parameter (stride ×1.6 per keep) — EVIDENCE: cancel is only checked per-parameter (line 419), not in the inner `while climbing` condition, so cancel latency on the main thread equals a full param climb. FIX: add `hooks.should_cancel()` to the while condition and treat it as climb-ending.