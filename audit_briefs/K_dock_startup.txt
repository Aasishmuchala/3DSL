- [P2] dock.py:91 — worker `except Exception` misses BaseException — TRIGGER: `SystemExit`/any BaseException inside the io `fn` — EVIDENCE: neither `done` nor `failed` is emitted, so `_run_blocking_io`'s `loop.exec()` (dock.py:434) never quits — a permanent modal block on Max's main thread; the cancel flag can never be re-checked — FIX: `except BaseException as e: self.failed.emit(str(e))`.

- [P1] maxgaffer/ui/dock.py:1242 — dock close→reopen shows nothing (re-registration path broken) — TRIGGER: user closes the floating dock panel with its X, then clicks the MaxGaffer toolbar action — EVIDENCE: `_dock_instance` stores the INNER widget (1242), never the QDockWidget wrapper (1234-1238); lines 1228-1229 call `.show()/.raise_()` on a child of the hidden dock, which cannot re-show it, and close-only-hides means the RuntimeError fallback (1231) never fires — macro looks dead until Max restart — FIX: store the QDockWidget and show/raise the wrapper on re-invoke.

- [P1] maxgaffer/ui/dock.py:474 — busy camera switch leaves the combo pointing at a camera that was never selected — TRIGGER: during MATCH/BOARD/REFINE, pick another camera in the header combo (it stays enabled; `_log`'s `processEvents()` at line 422 delivers the signal) — EVIDENCE: handler logs "busy — camera switch ignored" and returns, but the combo keeps the new index; the finally-block `refresh_cameras()` (446/458-459) preserves the wrong camera, so Locks ▾, score badge, rig sliders, Restore and Finals target camera B while scene/viewport/`_show_reference(cam)` still hold camera A — FIX: in the busy branch, `blockSignals(True)` + reset the combo index to the running camera.

- [P2] maxgaffer/startup/maxgaffer_startup.py:17 — `_repo_path()` assumes a dict and is called OUTSIDE `_register`'s try (line 23; try starts at 26) — TRIGGER: the same non-dict config.json at Max startup — EVIDENCE: AttributeError escapes `except (OSError, ValueError)` (line 18) and propagates uncaught out of the startup script → Max logs a startup-script error and the macroScript is never registered — plugin silently missing for the session — FIX: isinstance-dict check in `_repo_path`, and wrap the entire `_register` body.

- [P2] maxgaffer/startup/maxgaffer_startup.py:24 — macro registers even when the repo path is missing/moved — TRIGGER: config.json absent + MAXGAFFER env unset, or the clone relocated after install — EVIDENCE: the sys.path insert is skipped (repo falsy) but the macroScript still registers; clicking it raises ModuleNotFoundError inside `python.Execute` → raw MAXScript runtime-error dialog, with no warning printed at startup — FIX: print an explicit listener warning when repo is invalid, or make the macro body show a friendly message.

- [P2] maxgaffer/ui/dock.py:85 — parentless `_Worker` QThreads with no teardown/shutdown guard — TRIGGER: user exits Max mid-match or mid-vantage-job (close events ARE processed — `_log` calls `processEvents()` at line 422 and finals/matches spin nested loops) — EVIDENCE: workers are created with `super().__init__()` (no QObject parent) and held only by the Python list `self._workers`; no closeEvent/aboutToQuit/drain exists → a worker finalized while running = Qt fatal "QThread: Destroyed while thread is still running" abort at exit — FIX: parent workers to the dock, and on close set `_cancel` + `wait()` for in-flight workers.

- [P2] maxgaffer/ui/dock.py:524 — session `semantics` rendered with unchecked numeric formatting — TRIGGER: sidecar `<scene>.maxgaffer.json` carrying `"semantics": {"wb_kelvin_estimate": "warm"}` (hand edit/version skew — `CameraEntry.from_dict` only isinstance-checks the dict, session.py:68) — EVIDENCE: `f"{s.get('wb_kelvin_estimate', 0):.0f}K"` raises ValueError; `_show_reference` is called unguarded at line 486 (outside `_on_camera_combo`'s try) and at line 769 inside `_start_match`'s `finally`, where the exception escapes the slot after a successful run — FIX: coerce with try/float or format only when the value is numeric.

- [P0] maxgaffer/ui/dock.py:1194 + maxgaffer/core/omega.py:22 — "Test gateway" runs the full blocking `omega.call` chain on Max's MAIN thread inside a modal dialog — TRIGGER: Settings → "Test gateway" with the gateway unreachable (VPN off, black-holed packets, Cloudflare hanging). Even the fast-failure case (connection refused) blocks 2+6+15 = 23 s of `time.sleep`; the slow case is 4 attempts × 120 s timeout + sleeps ≈ 8.4 min — EVIDENCE: `_open_settings` uses `dlg.exec()` (dock.py:968); `_test` calls `omega.ping()` directly, bypassing the `ctrl.io` worker pump wired at dock.py:109, violating SPEC §2 Threading ("gateway… on workers via injectable io runner"); `omega.call` (omega.py:120-148) sleeps and `urlopen(timeout=TIMEOUT_S=120)` synchronously on whatever thread invokes it. Max goes Not Responding → force-kill → unsaved scene loss — FIX: route the ping through a `_Worker`/`_run_blocking_io`-style pump (disable the button while running), and give `call()` an optional short-timeout/fewer-retries profile for interactive pings.

- [P0] maxgaffer/ui/dock.py:1194 — SettingsDialog "Test gateway" runs a blocking network call on Max's main thread, violating SPEC §2's locked threading decision ("gateway on workers via injectable io runner") — TRIGGER: Settings → Test gateway while the gateway blackholes packets (VPN/firewall — exactly when users test) — EVIDENCE: `ping()` → `omega.call` with TIMEOUT_S=120 × 4 attempts + 23 s backoff (core/omega.py:22-23,120-149) executes inline, never via `ctrl.io`; Max's main thread hard-blocks up to ~8.4 min → "Not Responding" → force-kill loses the unsaved scene — FIX: pass the controller into the dialog and route through `dock._run_blocking_io`, or give ping a single-attempt ~10 s timeout.

- [P2] maxgaffer/ui/dock.py:969 — `self.cfg.save()` unguarded on Settings save — TRIGGER: read-only %LOCALAPPDATA% or an AV/Indexer lock on config.json when the user clicks Save — EVIDENCE: config.py:57-59 `open(CONFIG_PATH, "w")` raises OSError; escapes the slot → traceback, and the user believes settings persisted — FIX: try/except OSError with a log line or messageBox.

- [P1] maxgaffer/ui/dock.py:584 — `_pick_hdri` calls `ctrl.set_dome_hdri(path)` with no try/except, unlike every sibling handler — TRIGGER: dome light deleted (rig stale) between "Read scene" and RIG → HDRI… — EVIDENCE: `set_dome_hdri` → `self.rig()` / `sc.set_dome_texture` (controller.py:827-838) touch pymxs and can raise; the exception escapes the slot and the seed-release bookkeeping (591-597) is silently skipped — FIX: wrap in try/except + `self._log(f"✗ HDRI: {e}")`.

- [P1] maxgaffer/ui/dock.py:877 — `_restore_pre_match` calls `ctrl.restore_pre_match(cam)` unguarded — TRIGGER: a light present at snapshot time was deleted before the user hits Restore — EVIDENCE: `restore_pre_match` → `apply_state` (controller.py:857-878) raises mid-apply; exception escapes the slot AND the scene is left half-restored (mixed pre/post-match light) with no user-facing message — FIX: try/except + log, rebuild rig controls in a finally.

- [P2] maxgaffer/ui/dock.py:434 — `_run_blocking_io` nested event loop has no timeout/cancel escape — TRIGGER: Final ALL with `final_render_backend:"vantage_cli"` (`render_stills` timeout=3600/job, vantage.py:171), or any io fn that never returns, or raises BaseException (`SystemExit` bypasses `_Worker.run`'s `except Exception`, so neither signal fires and `loop.exec()` never quits) — EVIDENCE: `loop.exec(); w.wait()` with no `should_cancel` polling; the Cancel button is never enabled during finals (dock.py:922-941) — Max wedges in the nested loop up to an hour per job — FIX: poll `_cancel` with periodic `w.wait(100)` while pumping events, and enable Cancel for finals.