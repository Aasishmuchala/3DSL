- [P1] maxbridge/draft.py:74 — **draft mutates renderer props BEFORE writing the crash-safe snapshot — the module's stated contract is inverted** — TRIGGER: Max hard-crashes mid-loop in `apply_draft` (renderer prop sets trigger V-Ray re-init; this module exists precisely because crashes happen) — EVIDENCE: docstring says "Before touching anything, the original values are written to a crash-safe snapshot file", but `set_prop` runs inside the collection loop (line 74) and the file is written only after ALL props are mutated (line 82). A crash in between leaves 1–4 sampler props silently mutated with NO recovery file — the artist's noise threshold/subdivs/time-cap are lost and can ship in final renders. — FIX: two passes — collect all originals, write snapshot file, THEN apply draft values; on write failure nothing has been touched.

- [P2] maxbridge/apply.py:134,144,154 — **unguarded float() conversions inside the undo record** — TRIGGER: today unreachable (states are clamped floats via set()/from_dict), but any future code path writing a raw value into `state.values`/`state.groups` directly (public dicts) — EVIDENCE: `float(state.get(key))` (also `state.get("sun.enabled") >= 0.5` at :123) raises ValueError/TypeError inside `with pymxs.undo(...)`, propagating out of apply_state on Max's main thread; pymxs.undo's exception-time record handling is unpinned by any test (see untested). — FIX: wrap conversions in try/except and downgrade to warnings, matching the module's per-parameter fault-isolation contract.

- [P2] maxbridge/draft.py:74-77 — **set_prop result ignored: log claims a change that never happened** — TRIGGER: a renderer prop passes `isProperty` but its setattr fails (read-only/locked) — EVIDENCE: `set_prop` returns None, yet the snapshot entry was already recorded and the log unconditionally prints "draft: name old → new"; restore later "restores" a value that never changed. Cosmetic-misleading, not destructive. — FIX: check the return; only snapshot + log on success.

- [P0] maxbridge/exposure.py:80 — **ensure_exposure_control() silently replaces an existing non-V-Ray exposure control, with no snapshot, no undo, and a false log line** — TRIGGER: scene has a native Photographic/Logarithmic exposure control (non-VRay) + a non-physical camera (or no camera match) + `auto_exposure_control` (default True, config.py:35) → MATCH — EVIDENCE: `_find_exposure_control()` (line 63) only accepts `"vray" in classOf`, so a native EC reads as "absent"; line 80 then assigns `SceneExposureControl.exposureControl = <new VRay EC>`, clobbering the user's EC. The call (controller.py:411) sits OUTSIDE apply_state's undo record, the log claims "scene had no exposure host" (false), and pre_match is a LightingState (EV/WB numbers only) — Restore can never bring the original EC back. Artist's exposure setup permanently destroyed on a valid scene. — FIX: in `ensure_exposure_control`, return "host exists but unsupported" when `SceneExposureControl.exposureControl is not None` regardless of class; only auto-create when the slot is truly empty, and record the previous assignment in the session for Restore.

- [P2] maxbridge/exposure.py:100-115 + maxbridge/controller.py:410 — **host-chain order deviates from SPEC locked decision** — TRIGGER: scene with a native Physical camera and no exposure control — EVIDENCE: SPEC §2 locks the chain as "scene V-Ray EC (auto-created when absent) → native Physical → none", but auto-create only fires when `ExposureHost(cam).kind == "none"`, i.e. the physical camera is preferred OVER the auto-created EC. Code's order is EC-existing → physical cam → EC-auto-create → none. (Arguably saner for per-camera exposure, but it is a locked-decision deviation and belongs in the spec or a changelog, not silent code.) — FIX: either honor SPEC (auto-create EC whenever the slot is empty) or amend SPEC §2 to the implemented chain.

- [P2] maxbridge/draft.py:116 — **restore_draft raises uncaught ValueError on well-formed-but-string-typed snapshot values** — TRIGGER: hand-edited/third-party-written `draft_snapshot.json` with a quoted number (`{"options_progressiveNoiseThreshold": "0.05"}`) — EVIDENCE: `float("0.05")` succeeds inside the guard, `set_prop` succeeds, then `f"...{value:g}"` formats the ORIGINAL string with `:g` → ValueError outside the try. Consequences: dock launch path is guarded (dock.py:128, only logs), but the loop ABORTS mid-restore (remaining props unrestored), the snapshot file is never deleted so recovery re-fails at every launch, and the two unguarded call sites (draft.py:60 via controller.py:456, controller.py:517 finally) propagate the exception — the finally one also masks the match's own exception. — FIX: format the coerced float (`{float(value):g}`) inside the try, and move the os.remove into a finally.

- [P1] maxbridge/apply.py:33 — **renaming a dimmed light resurrects the 0-poisoning failure SPEC declares "structurally impossible"** — TRIGGER: MATCH dims group light "Spot_A" to factor 0 (multiplier 0.0) → artist renames it "Key_A" (normal mid-project hygiene) → next `rig(refresh=True)` — EVIDENCE: `capture_baselines` reads the NEW name at its current (dimmed) value 0.0; `Session.adopt_baselines` (session.py:94) adopts it as never-seen → baseline "Key_A" = 0.0 → `apply.py:153` writes `0.0 * factor = 0` forever — the light is dead until the user discovers `forget_baseline`. SPEC §2 Baselines claims adopt-once kills 0-poisoning; a rename defeats it. — FIX: on first adoption skip/zero-guard values ≤ epsilon with a log line ("declining to adopt 0.0 baseline for 'Key_A' — likely dimmed, not authored; use forget_baseline to force"), or document rename as a re-author event in the UI.