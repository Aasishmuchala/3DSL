- [P1] maxgaffer/core/genome.py:76-77,148-155 — corrupt sidecar/preset JSON crashes session load with uncaught ValueError — TRIGGER: `<scene>.maxgaffer.json` (or a preset file) containing `"sun.azimuth_deg": 1e999` (valid JSON, Python `json` parses to `inf`) or `Infinity`/`NaN` literal, or any string value like `"sun.altitude_deg": "warm"` (hand-edit/0-byte-crash recovery) — EVIDENCE: `LightingState.from_dict` → `clamp` → `_wrap_deg` → `math.fmod(inf, 360)` raises `ValueError: math domain error` (runtime-verified), and `float("warm")` raises; `Session.load` (session.py:112-116) only wraps `json.load` in try/except, the `CameraEntry.from_dict` call at session.py:119 is outside it, so the exception escapes through `Controller.session` (controller.py:64) on Max's main thread and kills the dock/session until the file is deleted; `preset_loads` (session.py:186) is likewise unguarded — FIX: wrap the per-camera/per-preset `from_dict` calls in try/except (skip the bad entry, log loudly), and/or make `clamp`/`from_dict` coerce non-finite/non-numeric values (`math.isfinite` check, drop or clamp instead of raising).

- [P2] maxgaffer/core/genome.py:154 — NaN on a wrap param is silently stored and propagated toward scene writes — TRIGGER: sidecar/preset JSON containing a `NaN` literal (Python `json.load` accepts it by default) for `sun.azimuth_deg`/`dome.rotation_deg` — EVIDENCE: runtime-verified `from_dict({'values': {'sun.azimuth_deg': nan}})` stores `nan` (`fmod(nan,360)=nan`, `nan < 0` is False); the NaN then round-trips into `to_dict`/json and into `apply_state` scene writes — FIX: `if not math.isfinite(v): reject-or-clamp` inside `clamp`/`from_dict`.

- [P2] maxgaffer/core/genome.py:125-128,158-165 — `LightingState.get` default `0.0` for absent keys makes `diff()` hallucinate changes for multiplier groups — TRIGGER: pre_match snapshot and current state with asymmetric group sets (light layer added/removed between snapshot and report, or a state loaded from an older sidecar) — EVIDENCE: `diff()` compares missing group as `0.0` vs `1.0` (neutral multiplier), so the "scene changed" report (controller.py:382-385) prints `group.X: 0.00 → 1.00` for a light the tool never touched — undermines the honesty guarantee of the report — FIX: skip keys absent from *both* states, or use the spec's neutral default (1.0 for `group.*`) as the absent-value sentinel in `diff`.

- [P0] maxgaffer/core/session.py:115-116 — Corrupt sidecar silently resets to empty, then auto-overwrites the recoverable file — TRIGGER: sidecar torn/truncated (Max crash during external sync, hand-edit mistake, concurrent writers) → `Session.load` returns an EMPTY session with no backup and no warning → the next `save_session()` fires automatically on any rig scan (controller.py:78-79) or match → the on-disk file, still human-recoverable text, is permanently replaced by the empty payload — EVIDENCE: `except (OSError, ValueError): return s` (fresh empty session); `save()` writes over `self.path` unconditionally; no `.bak`, no log; the corrupt-file test (test_session_omega.py:53) pins the silent behavior — FIX: on parse failure, copy the bad file to `<path>.corrupt.bak`, surface a loud warning through the log, and only overwrite after explicit user action.

- [P1] maxgaffer/core/session.py:117 — `Session.load` crashes on valid-JSON non-object top level — TRIGGER: sidecar containing `[]`, `null`, `"str"`, or `42` (hand-edited — the docstring advertises the file as human-editable — or written by another tool) → `d.get("cameras")` raises `AttributeError`, outside the try block — EVIDENCE: verified `AttributeError: 'list' object has no attribute 'get'`; propagates through `Controller.session` (controller.py:64) to unguarded callers like `_sync_score_badge` (dock.py:471) → dock `__init__` dies → `launch()` shows "failed to launch: AttributeError…" and the plugin is bricked for that scene until the file is found and deleted — FIX: `if not isinstance(d, dict): return s` after `json.load`.

- [P1] maxgaffer/core/session.py:119 + maxgaffer/core/genome.py:152 — One non-numeric state value in ANY camera kills the entire load — TRIGGER: `"state": {"values": {"sun.intensity": "bright"}}` or `null` in a hand-edited sidecar → `clamp()` → `float(v)` raises `ValueError`/`TypeError`, uncaught (the `except (OSError, ValueError)` at session.py:115 wraps only `json.load`, not `CameraEntry.from_dict`) — EVIDENCE: verified `ValueError: could not convert string to float: 'bright'` and `TypeError` on `None`; every other camera's state is lost with it — FIX: wrap `CameraEntry.from_dict` per-entry in try/except (skip bad camera, log it), and/or coerce-skip non-numeric values inside `LightingState.from_dict`.

- [P1] maxgaffer/core/genome.py:75-77 (admitted via session.py:64/69) — NaN survives load in wrapped angles and round-trips into the scene — TRIGGER: sidecar with `"sun.azimuth_deg": NaN` (Python's `json` accepts NaN literals, and `json.dump` writes them, so any NaN that ever lands in memory persists forever) → `_wrap_deg(nan)` returns nan (fmod(nan)=nan; `nan < 0` is False) → `LightingState` carries NaN → applied to the sun/dome transform on next apply — EVIDENCE: verified `state.get('sun.azimuth_deg')` is `nan` after load; non-wrap NaN silently clamps to `lo` (intensity → 0.05, wrong but bounded) — FIX: reject non-finite values in `clamp`/`from_dict` (`math.isfinite` check, drop or fall back to default).

- [P2] maxgaffer/core/session.py:20,129-134 — FORMAT_VERSION written but never read — TRIGGER: a sidecar written by a newer MaxGaffer (version 2+, schema change) opened by this build → loaded blindly with best-effort field mapping, then re-saved at version 1, silently dropping anything the old schema doesn't know — EVIDENCE: `version` is only ever written, never checked on load; no migration, no refuse-loudly path — FIX: on load, if `version > FORMAT_VERSION`, back up the file and either refuse loudly or load read-only.

- [P2] maxgaffer/core/session.py:136 — Concurrent writers share one tmp filename — TRIGGER: two 3ds Max instances with the same scene open (or dock + a stray API process) both call `save()` → both open `path + ".tmp"`; interleaved writes can tear the JSON, and `os.replace` while the other handle is open raises `PermissionError` → `save()` returns False silently (verified the shared-tmp sequence; torn output is timing-dependent) — EVIDENCE: single hardcoded tmp name, no lock, return value ignored by most callers — FIX: per-writer tmp name (pid suffix) + best-effort lock file, and log on save failure.

- [P1] maxbridge/apply.py:33 — **renaming a dimmed light resurrects the 0-poisoning failure SPEC declares "structurally impossible"** — TRIGGER: MATCH dims group light "Spot_A" to factor 0 (multiplier 0.0) → artist renames it "Key_A" (normal mid-project hygiene) → next `rig(refresh=True)` — EVIDENCE: `capture_baselines` reads the NEW name at its current (dimmed) value 0.0; `Session.adopt_baselines` (session.py:94) adopts it as never-seen → baseline "Key_A" = 0.0 → `apply.py:153` writes `0.0 * factor = 0` forever — the light is dead until the user discovers `forget_baseline`. SPEC §2 Baselines claims adopt-once kills 0-poisoning; a rename defeats it. — FIX: on first adoption skip/zero-guard values ≤ epsilon with a log line ("declining to adopt 0.0 baseline for 'Key_A' — likely dimmed, not authored; use forget_baseline to force"), or document rename as a re-author event in the UI.

- [P2] maxbridge/apply.py:83 vs 153 — **baseline 0.0 asymmetry between read and apply** — TRIGGER: a group light with an adopted baseline of 0.0 (authored-off light, or the rename case above) — EVIDENCE: read_state uses `baselines.get(name, 1.0) or 1.0` (0.0 → 1.0) while apply uses `baselines.get(name, 1.0)` (0.0 kept → 0×factor = 0 always); the group slider can never raise the light and read-back reports a collapsing factor, confusing the solver/UI. — FIX: pick one rule (recommend: treat 0.0 baseline as "adopt declined" per the P1 fix) and use it in both paths.